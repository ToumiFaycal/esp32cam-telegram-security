// Copy this file to "secrets.h" (same folder) and fill in your own values.
// secrets.h is listed in .gitignore and must NEVER be committed or shared.
#pragma once

#define WIFI_SSID     "your-wifi-name"        // must be a 2.4 GHz network
#define WIFI_PASSWORD "your-wifi-password"
#define BOT_TOKEN     "123456789:paste-the-token-from-BotFather-here"
#define CHAT_ID       "your-numeric-telegram-chat-id"
